# New lesson author packet

Status: draft. This seed is deliberately excluded from the catalog.

Read `../../AUTHORING.md` and one relevant neighboring lesson. Then record:

1. The one relationship the first screen lets a learner discover.
2. The mathematical domain and exact state model.
3. What persists when objects move, and what each gesture changes.
4. A contrasting case that defeats an attractive but incorrect rule.
5. Keyboard equivalents and the meaning of cancellation, rewind, and reduced motion.
6. Independent checks for the mathematical claim in `lesson.test.cjs`.
7. Direct input checks for important gestures in an exported document.

Replace the seed model/view, choose a recognizable glyph, complete the manifest,
and set its status to ready only when its checks pass. No catalog source edits
are required. Do not treat this placeholder motion as a finished lesson.
